{ Apple is so greedy that they don't even allow us to have a "donate" button
  linking to Patreon, on the grounds that all "purchases" must go through
  their in-app system. }
{$ifdef CASTLE_IOS}
  {$define HIDE_DONATE}
{$endif}
